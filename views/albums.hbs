

<h1 class="main-title">Albums</h1>

<div class="card-container">

{{#each artistsAlbums.body.items}}

<div class="card">
    <h2>{{name}}</h2>
    <img src="{{images.0.url}}" alt="">
    <a href="/tracks/{{id}}">View tracks</a>
</div>

{{/each}}

</div>